Split English and Japanese Sentence
=========================================

split with ``MeCab``.

Requirements
------------

* Python 2.7+
* MeCab

Usage
-----

To run each splitter,

::

  python xxx.py -r read_file -w write_file

